#!/bin/bash

./install targetFile

CASK=$HOME/.cask/bin/cask

if [ ! -e $CASK ]; then
    curl -fsSL https://raw.githubusercontent.com/cask/cask/master/go | python
fi

cd $HOME/.emacs.d/

cask install

